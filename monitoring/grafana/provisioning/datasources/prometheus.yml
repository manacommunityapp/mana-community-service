# Auto-provisions the Prometheus datasource so Grafana works on first boot.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus          # fixed uid so the provisioned dashboard binds to it
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
